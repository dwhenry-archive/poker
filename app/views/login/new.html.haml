%h1 Login

= form_tag login_path do
  %div
    = label_tag :name, 'Username'
    = text_field_tag :name, @player.name

  %div
    = label_tag :password
    = password_field_tag :password, ''

  - if response.code =~ /^4/
    .errors
      Invalid username/password


  .buttons
    = submit_tag 'Login'
    = link_to 'Create new player', new_player_path

